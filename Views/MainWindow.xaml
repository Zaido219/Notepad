<Window x:Class="NotepadApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NotepadApp.Views"
        mc:Ignorable="d"
        Title="Notepad App" Height="450" Width="800">
    
    <DockPanel LastChildFill="True">
        <Menu DockPanel.Dock="Top" Background="White">
            <MenuItem Header="_File">
                <MenuItem Header="_New" />
                <MenuItem Header="_Open..." />
                <MenuItem Header="_Save" />
                <MenuItem Header="Save _As..." />
                <Separator />
                <MenuItem Header="E_xit" />
            </MenuItem>
        </Menu>

        <StatusBar DockPanel.Dock="Bottom" Background="#F3F3F3">
            <StatusBarItem>
                <TextBlock Text="Ln 1, Col 1" Foreground="#555555" FontSize="11"/>
            </StatusBarItem>
        </StatusBar>

        <TextBox AcceptsReturn="True" 
                 AcceptsTab="True" 
                 TextWrapping="Wrap" 
                 VerticalScrollBarVisibility="Auto" 
                 HorizontalScrollBarVisibility="Auto"
                 BorderThickness="0"
                 Padding="8"
                 FontSize="14"
                 FontFamily="Consolas"/>
    </DockPanel>
</Window>